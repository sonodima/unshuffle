// PLACEHOLDER: implemented during the i18n pass. Keep the export and props.
// A compact button (current language) that opens a sheet listing the available
// languages by their own names; picking one calls setLocale().
export interface LanguagePickerProps {
  /** Icon-only button (tight headers). */
  compact?: boolean
  className?: string
}

export function LanguagePicker(_props: LanguagePickerProps) {
  return null
}
